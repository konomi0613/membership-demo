# キャッシュ戦略

## 方針
Netlify は ISR サポートが不完全なため、更新頻度で使い分け。

## 使い分け

### Webhook（更新頻度低）
- 運営メンバー
- カテゴリ

→ ビルド時に静的生成

### キャッシュなし（更新頻度高）
- お知らせ
- コース
- レッスン

→ 毎回 API 取得

## Vercel 移行時
`revalidate` で細かい制御が可能。